# Keeps the pinned GitHub Actions (full commit hashes in .github/workflows/ci.yml) up to date.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
