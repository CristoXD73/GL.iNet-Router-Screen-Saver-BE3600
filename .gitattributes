# The router's shell (BusyBox ash) breaks on Windows line endings: every
# script that runs on the router, and the macOS/Linux scripts, must stay LF no
# matter what Git's autocrlf setting is.
* text=auto eol=lf
*.sh text eol=lf
*.lua text eol=lf
router/** text eol=lf
setup/** text eol=lf

# The Windows launchers are the opposite: cmd.exe is happiest with CRLF.
*.cmd text eol=crlf
*.ps1 text eol=crlf

# The single-file downloads are served exactly as stored (their checksums are published),
# so git must never convert their line endings.
studio/downloads/* -text

# Never touch these.
*.bea binary
*.gz binary
*.png binary
router/usr/bin/be3600-player binary
